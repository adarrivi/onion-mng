package com.adarrivi;

import com.google.inject.AbstractModule;

public class ConfigurationModule extends AbstractModule {

    @Override
    protected void configure() {
        // bind(BoneManagementService.class).to(implementation)
    }
}
